---
 - hosts: 127.0.0.1
   connection: local
   remote_user: root
   become: yes
   tasks:
   - name: Apply Roles
     include_role:
       name: "{{ item }}"
     with_items:
       - "common"
       - "kids_workstation"